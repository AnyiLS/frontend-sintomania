import styled from "styled-components";
import tw from "twin.macro";

export const InstructionsContainer = styled.div`
    ${tw`px-[10px] mt-[20px]`}

    p {
        ${tw`text-[#1d569b] text-center mb-[20px] font-medium text-[15px]`}
    }
`;